{ "version": 2,
  "guards": { "vpn": { "comment": "vpn is up", "command": "true", "message": "connect the vpn" } },
  "projects": {
    "api": { "path": "api", "type": "backend", "runner": "make {task}", "guards": ["vpn"], "tasks": { "start": "make run" } },
    "web": { "path": "web", "type": "frontend", "runner": "npm run {task}" } } }
