source tests/e2e/lib.exp
crun config
must {config editor}
sleep 1
send "l"
sleep 1
# collapsed project form shows the titled block + the override summary (VAR=value, @peer)
must {Environment Overrides}
must {AWS_PROFILE}
send "\033"
want_exit 0
