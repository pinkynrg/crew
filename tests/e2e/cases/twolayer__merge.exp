# config.json overrides + local.json overrides merge; local WINS (BAR), config-only (FOO) still applies.
source tests/e2e/lib.exp
crun start env=x
must {enter run}
send "\r"
must {V=config-foo W=local-bar}
sleep 1
send "\033"
want_done
