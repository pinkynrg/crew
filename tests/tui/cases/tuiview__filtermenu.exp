# the log viewer's f menu (full-screen service multiselect).
source tests/tui/lib.exp
crun start env=x
must {enter run}
send "\r"
must {gamma line}
send "f"
must {Show logs for}
sleep 0.4
snap "viewer-filter-menu"
snapend
send "\r"
sleep 0.5
send "\033"
want_done
