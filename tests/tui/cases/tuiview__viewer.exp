# the log viewer streaming three fixed lines (prefix column, footer).
source tests/tui/lib.exp
crun start env=x
must {enter run}
send "\r"
must {gamma line}
sleep 0.5
snap
snapend
send "\033"
want_done
